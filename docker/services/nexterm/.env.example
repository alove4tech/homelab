# Nexterm environment
# Copy to .env to override defaults if desired.

# Host port for the web UI
NEXTERM_PORT=6989

# Log level: error, warn, info, verbose, debug
NEXTERM_LOG_LEVEL=info

# Encryption key for stored credentials (required, generate with: openssl rand -hex 32)
NEXTERM_ENCRYPTION_KEY=
