services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped

    # Resource limits for Raspberry Pi 400
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          memory: 256M

    env_file:
      - .env

    volumes:
      - vaultwarden-data:/data

    ports:
      - "80:80"      # HTTP (use reverse proxy with SSL in production)
      # NOTE: WebSocket is built into the main server since Vaultwarden 1.29+.
      # Uncomment below only if running an older version that needs a separate port.
      # - "3012:3012"  # WebSocket (legacy)

    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/alive"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

    networks:
      - vaultwarden-net

    # Security hardening
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp

volumes:
  vaultwarden-data:
    driver: local

networks:
  vaultwarden-net:
    driver: bridge
